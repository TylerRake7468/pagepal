<h2>Recommend "<%= @book.title %>"</h2>

<%= form_with model: [@book, @recommendation], local: true do |f| %>
  <div>
    <%= f.label :review %><br>
    <%= f.text_area :review, rows: 4 %>
  </div>

  <%= f.submit "Post Recommendation" %>
<% end %>

<%= link_to "Back to Book", book_path(@book) %>
